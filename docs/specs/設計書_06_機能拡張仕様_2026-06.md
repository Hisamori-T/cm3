# Construction Manager v3 — 機能拡張仕様書（2026-06）

**作成日**: 2026-06-04  
**確認者**: 平等 久盛（ひさ）  
**対象**: 2026-06-04 承認済み追加要件

---

## 0. 変更概要

| # | 機能 | 区分 | 優先度 |
|---|------|------|--------|
| 1 | 工事台帳 → 案件詳細マージ | 大規模改修 | ★★★ |
| 2 | 権限ロール 複数選択対応 | 破壊的変更 | ★★★ |
| 3 | 管理者設定ページ統合 | 大規模改修 | ★★★ |
| 4 | 印影設定機能 | 新規 | ★★☆ |
| 5 | 編集履歴 5W1H 表示 | UI改修 | ★★☆ |
| 6 | Excel インポートモーダル移動 | UI改修 | ★★☆ |
| 7 | 取決金額 ↔ 発注書自動連動 | 新規ロジック | ★★☆ |
| 8 | 残支払「済」表示 | UI改修 | ★★☆ |

---

## 1. 工事台帳 → 案件詳細マージ

### 1.1 設計方針

`/projects/[id]/ledger/page.tsx`（独立ページ）を廃止し、`/projects/[id]/page.tsx` の「詳細」タブに統合。
デザイン基準: `docs/base/Construction_Manager_v3/screens/project-detail.html`

### 1.2 ページ構造

```
proj-header
  sub-nav: 詳細 | QCDS | 業者見積 | 顧客見積 | 注文書 | 注文請書 | 請求書 | 進捗 | 編集履歴
  ※「工事台帳」タブ廃止・詳細タブに統合

ステータス変更セグメント（7段階）

2カラムグリッド (2fr 1fr)
  左カラム:
    - 案件情報カード（field-grid / インライン編集）
    - 担当者カード（field-grid）

  右カラム:
    - ① 工事台帳 承認スタンプ 5枠カード（NEW）← 工事価格の上
    - ② 工事価格サマリーカード（実行予算/取決見通/精算見通タブ）
        └ 末尾: 見積書 承認ステータス ミニパネル（1-10対応）

全幅エリア（グリッド外）:
    - 取決見通表 vtbl（全幅100%）← 現 ledger/page.tsx から移動
```

**削除**:
- クイックリンクカード
- `ProjectSubNav.tsx` の「工事台帳」タブ
- `/projects/[id]/ledger/page.tsx`

### 1.3 工事台帳 承認スタンプ 5枠

#### 承認ロール変更

| 旧 | 新 |
|----|-----|
| 社長 | 社長（変更なし） |
| 建築部長 | 建築部長（変更なし） |
| 経理 | 経理（変更なし） |
| 担当 | **現場担当** |
| — | **営業担当**（新規追加） |

#### DBマイグレーション

```sql
-- 既存の「担当」を「現場担当」に変更
UPDATE ledger_approvals SET role_label = '現場担当' WHERE role_label = '担当';

-- 全案件に「営業担当」枠を追加（display_order=4）
INSERT INTO ledger_approvals (project_id, role_label, display_order)
SELECT id, '営業担当', 4 FROM projects WHERE deleted_at IS NULL;

-- display_order 再採番（社長=0, 建築部長=1, 経理=2, 現場担当=3, 営業担当=4）
```

#### 承認フロー

- 各枠は**独立して**押印依頼を出す（一括依頼ではなく1枠ごと）
- 押印依頼先: **任意のユーザーを都度選択**（固定なし）
- 押印依頼 → 通知送信 → 指定ユーザーが承認 → `ledger_approvals.approved_at` 記録
- 押印依頼時に `approver_user_id` / `requested_by_id` / `requested_at` を記録（既存カラム）

#### UIコンポーネント

```
┌── 工事台帳 承認 ───────────────────────────────┐
│                              [全体状況: 2/5 完了] │
├──────────────────────────────────────────────────┤
│ 社長       [🔴 奴間 正人 ✓ 5/10]                  │
│ 建築部長   [⏳ 中山 太郎 (承認待ち)]               │
│ 経理       [  未依頼  ] [押印依頼▾]               │
│ 現場担当   [  未依頼  ] [押印依頼▾]               │
│ 営業担当   [  未依頼  ] [押印依頼▾]               │
└──────────────────────────────────────────────────┘
```

### 1.4 取決金額 ↔ 発注書自動連動

- `qcds_direct_works.agreed_amount` = 同一業者（`vendor_id`）の発注書合計
- 連動タイミング: 発注書作成・更新・削除時にバックエンドで自動計算
- **分割発注対応**: 同一業者への複数発注書の合計金額を取決金額に反映
- 手動上書きも許可（連動値を上書きした場合はアイコンで表示）

```python
# services/qcds_sync.py
async def sync_agreed_amount_from_orders(db, project_id, vendor_id):
    total = await db.scalar(
        select(func.sum(PurchaseOrder.total_amount))
        .where(PurchaseOrder.project_id == project_id)
        .where(PurchaseOrder.vendor_id == vendor_id)
        .where(PurchaseOrder.status != 'cancelled')
    )
    await db.execute(
        update(QCDSDirectWork)
        .where(QCDSDirectWork.qcds_id.in_(...))
        .where(QCDSDirectWork.vendor_id == vendor_id)
        .values(agreed_amount=total or 0)
    )
```

### 1.5 UI変更詳細

| 要件 | 実装内容 |
|------|---------|
| 1-6 支払開始月ドロップダウン | vtbl の「月別精算」列ヘッダー先頭行に配置 |
| 1-7 取決チェックボックス削除 | `agreement_checked` 列を非表示（DBカラムは維持） |
| 1-8 残支払「済」表示 | `payment_completed=TRUE` or 月別合計≥取決金額 → 「済」バッジ（緑） |
| 1-9 テーブル幅100% | `min-width` 撤廃、`width: 100%` on `.vtbl` |
| 1-10 見積書承認ステータス | summary card 末尾に `approval-mini` パネル（design準拠） |

---

## 2. 権限ロール 複数選択対応

### 2.1 DBスキーマ変更

```sql
-- users テーブルに roles 配列カラム追加
ALTER TABLE users ADD COLUMN roles userrole[] NOT NULL DEFAULT '{member}'::userrole[];

-- 既存データ移行: role → roles（単一要素配列）
UPDATE users SET roles = ARRAY[role]::userrole[];

-- インデックス（GINインデックスで高速検索）
CREATE INDEX ix_users_roles ON users USING GIN(roles);
```

`role` カラムは表示用の「主要ロール」として維持（廃止しない）。

### 2.2 Backend 権限チェック更新

```python
# shared/services/permissions.py
def has_role(user: UserModel, *required_roles: UserRole) -> bool:
    """ユーザーが指定ロールのうち少なくとも1つを持つか確認"""
    return any(r in user.roles for r in required_roles)

def require_roles(user: UserModel, *required_roles: UserRole):
    """指定ロールを持たない場合 403 を送出"""
    if not has_role(user, *required_roles):
        raise HTTPException(status_code=403, detail="権限がありません")
```

既存の `user.role == UserRole.admin` を `has_role(user, UserRole.admin, UserRole.super_admin)` に順次置換。

### 2.3 API スキーマ変更

```python
# schemas/user.py
class UserCreate(BaseModel):
    roles: list[UserRole] = [UserRole.member]  # 複数選択
    role: UserRole = UserRole.member            # 主要ロール（表示用）

class UserUpdate(BaseModel):
    roles: list[UserRole] | None = None
    role: UserRole | None = None
```

### 2.4 Frontend ユーザー管理 UI

- ロール選択: 単一 `<select>` → **チェックボックスグループ**
- 全ロールを表示し、複数チェック可能
- 主要ロール（表示用）: ラジオボタンで1つ選択
- ロールバッジ: ユーザー一覧に複数バッジ表示（最大3個、超過は "+N" 省略）

---

## 3. 管理者設定ページ統合

### 3.1 URL構成

| 旧URL | 新URL | 内容 |
|-------|-------|------|
| `/admin/users` | `/admin` (ユーザータブ) | ユーザー管理 |
| `/admin/company` | `/admin` (会社情報タブ) | 会社情報 |
| `/admin/stamp-tax` | `/admin` (印紙税表タブ) | 印紙税表 |
| `/admin/quote-conditions` | `/admin` (見積条件文タブ) | 見積条件文 |
| なし | `/admin` (承認ルートタブ) | 承認ルート設定 |
| なし | `/admin` (基本契約約款タブ) | 基本契約約款 |
| なし | `/admin` (QCDSテンプレートタブ) | QCDSテンプレート |
| なし | `/admin` (監査ログタブ) | 監査ログ閲覧 |
| なし | `/admin` (バックアップタブ) | バックアップ操作 |
| なし | `/admin` (システム状態タブ) | システム状態確認 |

### 3.2 管理ページレイアウト（admin.html準拠）

```
/admin ページ
├── 左ナビ（230px・admin.html .admin-nav）
│   ─── 組織 ───
│   ユーザー      → ユーザーCRUD + 印影設定（統合）
│   会社情報      → 旧 /admin/company
│   承認ルート    → 承認フロー設定
│   ─── マスタ ───
│   印紙税表      → 旧 /admin/stamp-tax
│   見積条件文    → 旧 /admin/quote-conditions
│   基本契約約款  → 注文書PDFの約款条文管理
│   QCDSテンプレート → 業種別経費率プリセット
│   ─── 運用 ───
│   監査ログ      → edit_histories 閲覧（CSV出力）
│   バックアップ  → pg_dump 実行 + DL
│   システム状態  → コンテナ稼働・API・DB確認
└── 右コンテンツ（左ナビ選択中のセクション）
```

### 3.3 各新規セクションの仕様

#### 承認ルート

```
全社承認フロー設定:
  Step 1: [ロール選択▾] → 例: manager, admin, super_admin のいずれか
  Step 2: [ロール選択▾] → 例: admin, super_admin のいずれか
  [+ ステップ追加]

※ 担当者（案件の操作アカウント）が全ステップの承認依頼を送信する
```

DBに `approval_route_config` テーブル（または `company_settings` にJSONBで保存）:
```json
{
  "quote_approval_steps": [
    { "step": 1, "required_roles": ["manager", "admin", "super_admin"] },
    { "step": 2, "required_roles": ["admin", "super_admin"] }
  ]
}
```

#### 基本契約約款

- 現在 `pdf_export.py` にハードコードされている約款条文（第1〜9条）をDBで管理
- RichText/MarkdownエディタでUI編集可能
- 変更時に注文書PDFに即時反映

#### QCDSテンプレート

- 業種別の保険料率・固定費テンプレートをDBで管理
- 新規案件作成時に「テンプレートを適用」ボタンでQCDSに初期値セット
- テーブル: `qcds_templates`（id, name, description, labor_rate, construction_rate, ...）

#### 監査ログ

- `edit_histories` テーブルを管理者が閲覧
- フィルタ: 日付範囲・ユーザー・エンティティ種別
- CSV出力: 全フィールドを文字化けしないUTF-8 BOM付きCSV

#### バックアップ

- 手動トリガー: `pg_dump` → Celeryタスクで非同期実行
- 完了後: ダウンロードリンクを表示（30分有効）
- 実行ログをページに表示

#### システム状態

- APIバージョン（`/api/v1/health` から取得）
- DBバージョン（PostgreSQL version）
- 各コンテナの稼働確認（health endpoint ping）
- Redisキュー状態（Celeryタスク数）

### 3.4 サイドバー変更

```
左ペイン「設定」セクション:
  現在: 自社情報設定（/admin/company）
  変更後: 管理者設定（/admin）← admin/super_admin のみ表示
```

---

## 4. 印影設定機能

### 4.1 DBスキーマ

```sql
ALTER TABLE users
  ADD COLUMN stamp_text  VARCHAR(10) NULL,
  ADD COLUMN stamp_style VARCHAR(20) NULL DEFAULT 'circle-red';
-- stamp_style: 'circle-red' | 'circle-navy' | 'square-red' | 'square-navy'
```

### 4.2 印影設定UI（stamp-settings.html準拠）

- **設置場所**: 管理者設定 > ユーザー > ユーザー行の「印影」ボタン → モーダル
- **機能**:
  - 印影テキスト入力（漢字2〜4文字）
  - スタイル選択（4種類、ビジュアルプレビュー付き）
  - リアルタイムプレビュー（200×200px円形印）
  - 使用される場所のサムネイル一覧（見積書・注文書・注文請書・工事台帳）

### 4.3 印影の自動反映

設定した印影は以下の箇所に自動反映:
- 顧客見積書 承認スタンプ（担当/査閲/承認）
- 注文書 PDF承認欄
- 注文請書 PDF承認欄
- 工事台帳 承認スタンプ5枠

反映方法: ユーザーIDから `stamp_text` / `stamp_style` を動的に取得してスタンプUIを描画。

---

## 5. 編集履歴 5W1H 表示

### 5.1 表示形式

```
┌── 2026/06/03 14:32 ─────────────────────────────┐
│  誰が    平等 久盛（営業担当）                     │
│  何を    案件ステータス を変更                     │
│  いつ    2026/06/03 14:32                         │
│  どこで  案件「三国第二工場 配管改修」             │
│  どのように  「受注」→「着工」                    │
└─────────────────────────────────────────────────┘
```

「なぜ（変更理由）」は表示しない（DBに未記録のため）。

### 5.2 実装方針

- `edit_histories.field_changes` JSONB から既存データを5W1H形式にマッピング
- `entity_type` → 「どこで」のラベル変換
- `change_type` → 「何を」のラベル変換
- `field_changes.before/after` → 「どのように」のdiff表示

---

## 6. Excelインポートの移動

### 6.1 UI変更

```
/projects ページ（案件一覧）:
  Before: [新規案件 ＋]
  After:  [新規案件 ＋] [Excelインポート ↑]

サイドバー: 「Excelインポート」ナビを削除
/admin/import ページ: 廃止（モーダルに統合）
```

### 6.2 実装

- 「Excelインポート」ボタン → モーダルを開く
- モーダル内容: 現在の `/admin/import/page.tsx` のD&Dコンポーネントをそのまま使用
- インポート完了後: `router.refresh()` で案件一覧を再取得

---

## 7. 実装順序

```
Phase 1（DB基盤・今セッション）:
  ① migration: users.roles[], stamp_text/stamp_style
  ② migration: ledger_approvals 5枠変更
  ③ backend: User model, schema, 権限ヘルパー更新
  ④ backend: Admin API統合
  ⑤ frontend: /admin ページ（admin.html準拠）
  ⑥ frontend: 複数ロール選択UI

Phase 2（フロント主体・次セッション）:
  ⑦ frontend: 工事台帳→案件詳細マージ（project-detail.html準拠）
  ⑧ frontend: 編集履歴 5W1H
  ⑨ frontend: Excelインポートモーダル移動

Phase 3（新規機能）:
  ⑩ 取決金額↔発注書連動ロジック
  ⑪ 承認ルート設定（admin）
  ⑫ 基本契約約款管理
  ⑬ QCDSテンプレート
  ⑭ 監査ログ・バックアップ・システム状態
```

---

## 8. 請求書 — 総額請求書＋分割連動型（2026-06-06 承認）

### 8.1 概要

現行の「分割請求書 n 枚」方式を廃止し、
**総額請求書（親）＋分割請求書 1〜n 枚（子）** の親子構造に変更する。  
入金管理は総額請求書のみで行い、各分割への自動連動を実装する。

### 8.2 DBスキーマ変更

```sql
-- migration: invoice_split_v2

ALTER TABLE invoices
  ADD COLUMN invoice_type VARCHAR(20) NOT NULL DEFAULT 'standalone',
  -- 'standalone' | 'total' | 'split'
  ADD COLUMN parent_invoice_id UUID REFERENCES invoices(id) ON DELETE SET NULL;

ALTER TABLE payments
  ADD COLUMN target_split_id UUID REFERENCES invoices(id) ON DELETE SET NULL;
  -- どの回（split）の入金かを記録
```

### 8.3 請求書タイプ定義

| `invoice_type` | 説明 | 入金記録 | 削除可否 |
|---|---|---|---|
| `standalone` | 既存の通常請求書（変更なし）| ○ | 入金済以外 |
| `total` | 総額請求書（分割の親）| ○ **のみここで記録** | 子が全て未入金の場合 |
| `split` | 分割請求書（子、n 枚）| × 表示のみ | 総額請求書から自動更新 |

### 8.4 自動分割フロー

```
1. 請求書詳細で billing_method=percentage, billing_percentage=25 を設定・保存
2. 「自動分割」ボタン押下
3. API: POST /invoices/{id}/auto-split（既存エンドポイントを拡張）
   └── 現在の請求書 → invoice_type='total' に変換
       total_amount = 顧客見積 subtotal（税込）
   └── split 1〜4 を作成（parent_invoice_id = 総額ID）
       各 current_purchase = floor(subtotal_excl × pct / 100)
       最終回は端数込み
4. 一覧にて: 総額請求書を親行、分割 1〜n を折りたたみ子行で表示
```

### 8.5 入金記録フロー（total 請求書のみ）

**入金フォーム項目**:
- 入金日 *
- 金額（円）*
- **対象回** ← 新規: 「第1回（¥XXX）」「第2回（¥XXX）」ドロップダウン
- 支払方法
- 備考

**保存後の自動連動処理**:

```python
def on_payment_saved(payment):
    split_inv = payment.target_split  # 対象の分割請求書
    split_inv.status = 'paid' if payment.amount >= split_inv.total_amount else 'partially_paid'

    next_split = get_next_split(split_inv)  # 次の回の分割請求書
    if next_split:
        next_split.previous_balance = split_inv.total_amount
        next_split.received_amount  = payment.amount
        # 未払い分を次回に繰り越し
        unpaid = split_inv.total_amount - payment.amount
        next_split.current_purchase = next_split.billing_split_amount + unpaid
        _calc_totals(next_split)
```

**入金なし（前払なし）の場合**:  
次回の `current_purchase` = 前回の `billing_split_amount`（今月分）+ 前回の未払額

### 8.6 UIまとめ

#### 請求書一覧（/invoice）
```
📄 INV-001（総額）          ¥2,949,720（税込）   入金待ち
  ↳ INV-001-1 第1回/全4回   ¥737,430（25%）     入金済
  ↳ INV-001-2 第2回/全4回   ¥737,430（25%）     送付済
  ↳ INV-001-3 第3回/全4回   ¥737,430（25%）     下書き
  ↳ INV-001-4 第4回/全4回   ¥737,430（25%）     下書き
```

#### 請求書詳細（total）
- 入金記録フォームに「対象回」セレクト追加
- 分割請求書サマリ表（回・金額・ステータス・既入金）を下部に表示
- 分割ステータスが全て paid → 総額請求書も自動で paid に変更

#### 請求書詳細（split）
- ヘッダーに「第N回 / 全M回 · 総額請求書: INV-001」のリンク表示
- 入金記録セクションを非表示（総額請求書で管理）
- 読み取り専用（保存ボタンなし）

### 8.7 移行方針

- 既存の standalone 請求書: 変更なし
- 既存の split_total 設定済み請求書（前実装）: invoice_type='standalone' のまま維持
  → 新しい auto-split 実行時のみ total/split 構造に変換

### 8.8 実装ファイル

| ファイル | 変更内容 |
|---|---|
| `migration: invoice_split_v2` | invoice_type, parent_invoice_id, target_split_id 追加 |
| `models/invoice.py` | 上記カラム追加、parent/children relationship |
| `schemas/invoice.py` | InvoiceRead に invoice_type, parent_invoice_id, children[] 追加 |
| `modules/report/routers/invoices.py` | auto-split 拡張、payment 保存時の連動ロジック |
| `types/invoice.ts` | InvoiceType, children フィールド追加 |
| `invoice/page.tsx` | 総額→子行折りたたみ表示 |
| `invoice/[invoice_id]/page.tsx` | total/split 別UI、対象回セレクト |

---

## 9. 請求書詳細 — PDF明細追記行・合計計算修正（2026-06-05 実装済み）

### 9.1 追記行（PDF明細欄 追記行）

請求書詳細ページに「PDF 明細欄 追記行」セクションを追加。  
工事名行の直下に任意の追加行（追加工事・補足明細等）を記入できる。

**画面レイアウト（追記行グリッド）**:
```
日付(145px) | 工事名・備考(1fr) | 金額(120px) | 摘要(140px) | 削除(28px)
```
- 日付列: `<input type="date">` + `onClick → showPicker()` でカレンダーを確実に開く
- 先頭固定行: プロジェクト工事名（自動）・current_purchase・n/n回（編集不可）
- 追記行: 行追加/削除、保存時に items[] として PATCH

**DB保存**:
- `invoice_items.item_name` ← 工事名・備考
- `invoice_items.amount` ← 金額（任意）
- `invoice_items.remarks` ← 摘要（任意）
- `invoice_items.description` ← 日付文字列（YYYY-MM-DD）

### 9.2 工事完了日（`completion_date`）

`invoices.completion_date DATE` フィールド。  
PDF日付列の1行目（固定行）に表示。未入力の場合は空欄。  
入力欄は 180px 幅・`showPicker()` 対応。

### 9.3 合計計算ルール（`_calc_totals` 修正）

```python
# modules/report/routers/invoices.py
def _calc_totals(inv: Invoice, extra_amount: float = 0.0) -> None:
    purchase = float(inv.current_purchase or 0) + extra_amount
    inv.outstanding_balance = float(inv.previous_balance or 0) - float(inv.received_amount or 0)
    inv.tax_amount = floor(purchase * 0.10)
    inv.total_amount = purchase + floor(purchase * 0.10)
```

PATCH 保存時に `body.items` の金額合計を `extra_amount` として渡すことで、  
**`total_amount` = `current_purchase` + Σ(items[].amount) + 消費税10%** となる。

### 9.4 フロントエンド金額表示

追記行がある場合、金額セクションに小計内訳を表示する:
```
今月御請求額（税抜・円）  ¥737,430  ← current_purchase（手入力）
─────────────────────────────────
追記行合計（税抜）         + ¥100,000  ← Σ(extraRows.amount)（追記行あり時のみ表示）
小計（税抜）               ¥837,430
消費税 (10%)               ¥83,743
合計（税込）               ¥921,173
```

### 9.5 PDF出力（`_render_invoice_html` 修正）

```python
# pdf_export.py: 追記行の金額を合計に加算
extra_total = Σ(item.amount for item in invoice.items)
subtotal_all = current_purchase + extra_total
tax_amount   = int(subtotal_all * 0.10)
total        = subtotal_all + tax_amount
```
- 6列サマリ「当月御買上額」: `subtotal_all` を使用
- 明細テーブル「計」行: `subtotal_all` を使用
- 発行日（右上）: `yr`/`mo`/`dy` を `issue_date` から分割して定義（500エラー修正）

### 9.6 実装ファイル

| ファイル | 変更内容 |
|---|---|
| `modules/report/routers/invoices.py` | `_calc_totals(extra_amount)` 引数追加、PATCH で items 金額加算 |
| `modules/report/services/pdf_export.py` | `subtotal_all` 計算、`yr/mo/dy` 定義、6列サマリ・計行修正 |
| `invoice/[invoice_id]/page.tsx` | 追記行UI、日付列145px、showPicker、金額小計内訳表示 |

---

## 10. 請求書システム Phase R-1 — 出来高・控除・支払通知書（2026-06-08 設計確定）

**確認者**: 平等 久盛（ひさ）  
**確定日**: 2026-06-08  
**前提**: 設計レビュー（Pros/Cons）完了・全3質問の回答確定済み

### 10.0 スコープ確定

| 機能 | R-1 | R-2 | R-3 |
|---|---|---|---|
| `project_role` 追加 + 一覧バッジ/絞り込み | ✅ | | |
| `invoice_phase` / 発行時スナップショット | ✅ | | |
| `invoice_deductions` テーブル（正規化） | ✅ | | |
| 出来高入力 UI（下請フロー） | ✅ | | |
| 控除入力 UI（元請フロー） | ✅ | | |
| 支払通知書 PDF | ✅ | | |
| `account_hint` UI 入力 | | | ✅ |
| freee/MF 連携 CSV | | ✅ | |
| 法定福利費・仕訳 CSV | | | ✅ |

---

### 10.1 新 Enum 定義

既存 `backend/app/shared/models/enums.py` に追加する。

```python
class ProjectRole(str, enum.Enum):
    prime  = "prime"   # 元請
    sub    = "sub"     # 下請
    public = "public"  # 公共

class InvoicePhase(str, enum.Enum):
    advance  = "advance"   # 前払金
    interim  = "interim"   # 中間前払金
    partial  = "partial"   # 部分払
    final    = "final"     # 完成払 / 精算
    none     = "none"      # フェーズ指定なし（既存請求書との互換）

class DeductionType(str, enum.Enum):
    safety_fee         = "safety_fee"          # 安全協力会費
    materials_advance  = "materials_advance"   # 材料費立替
    parking_fee        = "parking_fee"         # 駐車場代
    statutory_welfare  = "statutory_welfare"   # 法定福利費
    other              = "other"               # その他
```

`backend/app/models/enums.py` の re-export リストにも追加する。

---

### 10.2 DB スキーマ差分

#### 10.2.1 `projects` テーブル — 追加カラム

```sql
ALTER TABLE projects
  ADD COLUMN project_role VARCHAR(10) DEFAULT 'prime';
-- 既存データは 'prime' でデフォルト補完（後から手動変更可）
```

#### 10.2.2 `invoices` テーブル — 追加カラム

```sql
ALTER TABLE invoices
  ADD COLUMN invoice_phase             VARCHAR(20) DEFAULT 'none',
  ADD COLUMN project_role_snapshot     VARCHAR(10),
  ADD COLUMN contract_amount_snapshot  NUMERIC(12,0),
  ADD COLUMN total_deduction_amount    NUMERIC(12,0) DEFAULT 0,
  ADD COLUMN final_payable_amount      NUMERIC(12,0) DEFAULT 0;
-- total_deduction_amount: 控除合計（invoice_deductions から集計して保存）
-- final_payable_amount  : 支払決定額 = total_amount - total_deduction_amount
-- スナップショットは発行時（status: draft→sent）に自動転記
```

#### 10.2.3 `invoice_deductions` テーブル — 新規作成

```sql
CREATE TABLE invoice_deductions (
  id                UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id        UUID         NOT NULL REFERENCES invoices(id) ON DELETE CASCADE,
  deduction_type    VARCHAR(30)  NOT NULL,  -- DeductionType enum値
  description       VARCHAR(255),            -- 表示ラベル（enum デフォルトを上書き可）
  amount            NUMERIC(12,0) NOT NULL,
  calculation_rate  NUMERIC(5,4),            -- auto 計算の場合の率（例: 0.005 = 0.5%）
  account_hint      VARCHAR(50),             -- 会計連携用（R-3 で UI 追加、今は空）
  is_deleted        BOOLEAN      NOT NULL DEFAULT FALSE,
  row_no            INTEGER      NOT NULL DEFAULT 0,
  created_at        TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
  updated_at        TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_invoice_deductions_invoice_id ON invoice_deductions(invoice_id)
  WHERE is_deleted = FALSE;
```

**論理削除**: `is_deleted = TRUE` で管理。物理削除は行わない（CLAUDE.md ルール準拠）。  
**変更履歴**: 親 `Invoice` の `edit_histories` に控除追加/削除を記録する。

---

### 10.3 SQLAlchemy モデル定義（2.0 スタイル）

> 既存 `backend/app/models/invoice.py` の `Invoice` クラスに列を追加し、  
> `InvoiceDeduction` クラスを同ファイルに追記する。

#### `Invoice` クラス — 追加列

```python
# backend/app/models/invoice.py（追加分のみ）
from app.models.enums import InvoicePhase, ProjectRole  # 新 enum を import

class Invoice(Base, TimestampMixin):
    # ... 既存フィールドは変更なし ...

    # ── Phase R-1: 出来高・フェーズ・スナップショット ─────────────────────
    invoice_phase: Mapped[InvoicePhase] = mapped_column(
        SAEnum(InvoicePhase, name="invoicephase"),
        nullable=False, default=InvoicePhase.none,
    )
    project_role_snapshot: Mapped[str | None] = mapped_column(
        String(10), nullable=True,
        comment="発行時の案件立場スナップショット（prime/sub/public）",
    )
    contract_amount_snapshot: Mapped[float | None] = mapped_column(
        Numeric(12, 0), nullable=True,
        comment="発行時の契約金額スナップショット",
    )
    total_deduction_amount: Mapped[float] = mapped_column(
        Numeric(12, 0), nullable=False, default=0,
        comment="控除合計（invoice_deductions から集計して保存）",
    )
    final_payable_amount: Mapped[float] = mapped_column(
        Numeric(12, 0), nullable=False, default=0,
        comment="支払決定額 = total_amount - total_deduction_amount",
    )

    # relationship
    deductions: Mapped[list["InvoiceDeduction"]] = relationship(
        "InvoiceDeduction",
        primaryjoin="and_(InvoiceDeduction.invoice_id == Invoice.id, "
                    "InvoiceDeduction.is_deleted == False)",
        back_populates="invoice",
        order_by="InvoiceDeduction.row_no",
    )
```

#### `InvoiceDeduction` クラス — 新規追加

```python
class InvoiceDeduction(Base, TimestampMixin):
    """元請が下請への支払時に天引きする控除項目。論理削除で管理。"""

    __tablename__ = "invoice_deductions"

    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, default=uuid.uuid4,
    )
    invoice_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), ForeignKey("invoices.id", ondelete="CASCADE"), nullable=False,
    )
    deduction_type: Mapped[DeductionType] = mapped_column(
        SAEnum(DeductionType, name="deductiontype"), nullable=False,
    )
    description: Mapped[str | None] = mapped_column(
        String(255), nullable=True,
        comment="表示ラベル。未入力時は deduction_type の日本語名をデフォルト表示",
    )
    amount: Mapped[float] = mapped_column(Numeric(12, 0), nullable=False)
    calculation_rate: Mapped[float | None] = mapped_column(
        Numeric(5, 4), nullable=True,
        comment="率計算の場合のレート（例: 0.005 = 0.5%）。NULL = 手動入力",
    )
    account_hint: Mapped[str | None] = mapped_column(
        String(50), nullable=True,
        comment="会計連携用の勘定科目ヒント（R-3 フェーズで UI 追加）",
    )
    is_deleted: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    row_no: Mapped[int] = mapped_column(Integer, nullable=False, default=0)

    # relationship
    invoice: Mapped["Invoice"] = relationship("Invoice", back_populates="deductions")
```

#### `Project` クラス — 追加列

```python
# backend/app/models/project.py（追加分のみ）
project_role: Mapped[str] = mapped_column(
    String(10), nullable=False, default="prime",
    comment="案件立場: prime | sub | public",
)
```

---

### 10.4 Alembic マイグレーション

```
backend/alembic/versions/R1_invoice_role_phase_deductions.py
```

```python
"""Phase R-1: project_role / invoice_phase / invoice_deductions."""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = 'r1a2b3c4d5e6'
down_revision = 'v6w7x8y9z0a1'  # 直前のマイグレーション ID に合わせる
branch_labels = None
depends_on = None


def upgrade() -> None:
    # 1. Enum 追加
    op.execute("CREATE TYPE invoicephase AS ENUM ('advance','interim','partial','final','none')")
    op.execute("CREATE TYPE deductiontype AS ENUM ('safety_fee','materials_advance','parking_fee','statutory_welfare','other')")

    # 2. projects テーブル
    op.add_column('projects', sa.Column('project_role', sa.String(10), nullable=False, server_default='prime'))

    # 3. invoices テーブル
    op.add_column('invoices', sa.Column('invoice_phase', postgresql.ENUM('advance','interim','partial','final','none', name='invoicephase', create_type=False), nullable=False, server_default='none'))
    op.add_column('invoices', sa.Column('project_role_snapshot',    sa.String(10),        nullable=True))
    op.add_column('invoices', sa.Column('contract_amount_snapshot', sa.Numeric(12, 0),    nullable=True))
    op.add_column('invoices', sa.Column('total_deduction_amount',   sa.Numeric(12, 0),    nullable=False, server_default='0'))
    op.add_column('invoices', sa.Column('final_payable_amount',     sa.Numeric(12, 0),    nullable=False, server_default='0'))

    # 4. invoice_deductions テーブル
    op.create_table('invoice_deductions',
        sa.Column('id',               postgresql.UUID(as_uuid=True), primary_key=True),
        sa.Column('invoice_id',       postgresql.UUID(as_uuid=True), sa.ForeignKey('invoices.id', ondelete='CASCADE'), nullable=False),
        sa.Column('deduction_type',   postgresql.ENUM('safety_fee','materials_advance','parking_fee','statutory_welfare','other', name='deductiontype', create_type=False), nullable=False),
        sa.Column('description',      sa.String(255),  nullable=True),
        sa.Column('amount',           sa.Numeric(12,0), nullable=False),
        sa.Column('calculation_rate', sa.Numeric(5,4),  nullable=True),
        sa.Column('account_hint',     sa.String(50),   nullable=True),
        sa.Column('is_deleted',       sa.Boolean,      nullable=False, server_default='false'),
        sa.Column('row_no',           sa.Integer,      nullable=False, server_default='0'),
        sa.Column('created_at',       sa.TIMESTAMP(timezone=True), server_default=sa.text('NOW()')),
        sa.Column('updated_at',       sa.TIMESTAMP(timezone=True), server_default=sa.text('NOW()')),
    )
    op.create_index('idx_invoice_deductions_invoice_id', 'invoice_deductions', ['invoice_id'],
                    postgresql_where=sa.text('is_deleted = FALSE'))


def downgrade() -> None:
    op.drop_index('idx_invoice_deductions_invoice_id', 'invoice_deductions')
    op.drop_table('invoice_deductions')
    op.drop_column('invoices', 'final_payable_amount')
    op.drop_column('invoices', 'total_deduction_amount')
    op.drop_column('invoices', 'contract_amount_snapshot')
    op.drop_column('invoices', 'project_role_snapshot')
    op.drop_column('invoices', 'invoice_phase')
    op.drop_column('projects', 'project_role')
    op.execute("DROP TYPE IF EXISTS deductiontype")
    op.execute("DROP TYPE IF EXISTS invoicephase")
```

---

### 10.5 新規 API エンドポイント

#### 10.5.1 案件立場変更

```
PATCH /api/v1/projects/{project_id}/role
Body: { "project_role": "prime" | "sub" | "public" }
Response: ProjectRead（project_role 含む）
権限: admin または 作成者本人のみ
```

#### 10.5.2 出来高サマリー（動的計算）

```
GET /api/v1/projects/{project_id}/invoices/progress-summary
Response:
{
  "contract_amount": 10000000,
  "cumulative_billed": 3000000,   // 過去の invoices.current_purchase 合計
  "outstanding_contract": 5000000  // contract_amount - cumulative_billed - 最新draft.current_purchase
}
```

※ `cumulative_billed` は保存せず毎回計算（データ不整合防止）

#### 10.5.3 控除 CRUD

```
POST   /api/v1/invoices/{invoice_id}/deductions
Body:
{
  "deduction_type": "safety_fee",
  "description": null,              // 未入力なら enum の日本語名を使用
  "amount": null,                   // calculation_rate が null でない場合は自動計算
  "calculation_rate": 0.005         // 0.5% → amount = invoice.current_purchase × 0.005
}
Response: InvoiceDeductionRead
制約: invoice.status == "draft" のみ操作可能

PATCH  /api/v1/invoices/{invoice_id}/deductions/{deduction_id}
Body: { "amount": 11000, "description": "安全協力会費（5月分）" }

DELETE /api/v1/invoices/{invoice_id}/deductions/{deduction_id}
→ is_deleted = TRUE（論理削除）
制約: invoice.status == "draft" のみ操作可能
```

控除追加/削除のたびに `invoices.total_deduction_amount` / `final_payable_amount` を再計算して保存する。

#### 10.5.4 支払通知書 PDF

```
GET /api/v1/invoices/{invoice_id}/payment-notice-pdf
Response: StreamingResponse (application/pdf)
ファイル名: 支払通知書_{invoice_number}.pdf
```

#### 10.5.5 発行時スナップショット転記

既存の invoice status 変更 (`PATCH /invoices/{id}`) フックに追記する：

```python
# backend/app/modules/report/routers/invoices.py
if body.status in ("sent", "paid") and invoice.status == "draft":
    invoice.project_role_snapshot    = project.project_role
    invoice.contract_amount_snapshot = project.project_price
```

---

### 10.6 Pydantic スキーマ（追加/変更分）

```python
# backend/app/schemas/invoice.py

class InvoiceDeductionCreate(BaseModel):
    deduction_type: DeductionType
    description: str | None = None
    amount: float | None = None          # calculation_rate 指定時は None でも可
    calculation_rate: float | None = None

class InvoiceDeductionRead(BaseModel):
    id: uuid.UUID
    invoice_id: uuid.UUID
    deduction_type: DeductionType
    description: str | None
    amount: float
    calculation_rate: float | None
    account_hint: str | None
    row_no: int
    model_config = ConfigDict(from_attributes=True)

# InvoiceRead に追加
class InvoiceRead(BaseModel):
    # ... 既存フィールド ...
    invoice_phase: InvoicePhase
    project_role_snapshot: str | None
    contract_amount_snapshot: float | None
    total_deduction_amount: float
    final_payable_amount: float
    deductions: list[InvoiceDeductionRead] = []

# ProjectRead / ProjectUpdate に追加
class ProjectRead(BaseModel):
    # ... 既存フィールド ...
    project_role: str  # 'prime' | 'sub' | 'public'
```

---

### 10.7 サービス層 — 控除計算ロジック

```python
# backend/app/modules/report/services/deduction_service.py

_DEDUCTION_LABEL_JA = {
    "safety_fee":         "安全協力会費",
    "materials_advance":  "材料費立替",
    "parking_fee":        "駐車場代",
    "statutory_welfare":  "法定福利費",
    "other":              "その他",
}

def calculate_deduction_amount(
    deduction_type: str,
    calculation_rate: float | None,
    amount: float | None,
    invoice_current_purchase: float,
) -> float:
    """控除金額を計算する。rate 指定時は自動計算、それ以外は amount を使用。"""
    if calculation_rate is not None:
        return round(invoice_current_purchase * calculation_rate)
    if amount is not None:
        return amount
    raise ValueError("amount または calculation_rate のどちらかが必要です")


def recalculate_invoice_deduction_totals(invoice: Invoice) -> None:
    """invoice_deductions の合計を再計算して Invoice に反映する。"""
    active = [d for d in invoice.deductions if not d.is_deleted]
    total_deduction = sum(d.amount for d in active)
    invoice.total_deduction_amount = total_deduction
    invoice.final_payable_amount   = float(invoice.total_amount or 0) - total_deduction
```

---

### 10.8 フロントエンド UI 仕様

#### 10.8.1 案件一覧 — 役割バッジ＋絞り込み

```
projects/page.tsx に追加:

[元請] [下請] [公共] [すべて]  ← タブ or プルダウン（URL パラメータ ?role=prime）

一覧カードの工事番号横に小バッジ表示:
  prime  → 「元請」（青）
  sub    → 「下請」（橙）
  public → 「公共」（緑）
```

#### 10.8.2 出来高入力（下請フロー）

> 請求書詳細ページ（`invoice/[invoice_id]/page.tsx`）に、`project.project_role === 'sub'` のみ表示するセクションを追加。

```
■ 出来高（下請請求）
─────────────────────────────────────────────────
契約金額（税抜）   ¥10,000,000    ← contract_amount_snapshot
前回累計請求額     ¥ 3,000,000    ← cumulative_billed (API)
                   ───────────────
今回出来高 [ 20.0 ] %  ⟺  ¥ [ 2,000,000 ]   ← 双方向連動
請求残高（税抜）   ¥ 5,000,000    ← outstanding_contract (自動)
─────────────────────────────────────────────────
```

**双方向連動ロジック**（フロントエンド側）:
- % 変更時: `amount = Math.round(contract_amount_snapshot × percent / 100)`
- 金額変更時: `percent = (amount / contract_amount_snapshot × 100).toFixed(1)`

#### 10.8.3 控除入力（元請フロー）

> 請求書詳細ページに、`project.project_role === 'prime'` のみ表示するセクションを追加。

```
■ 控除項目（天引き）
─────────────────────────────────────────────────
[種別プルダウン▼] [計算方法: 自動(%)|手動] [金額]  [追加]

  1. 安全協力会費     0.5% 自動   ¥11,000   [✏] [🗑]
  2. 材料費立替       手動        ¥150,000  [✏] [🗑]
  3. 駐車場代         手動        ¥20,000   [✏] [🗑]
─────────────────────────────────────────────────
控除合計              ¥181,000
支払決定額            ¥2,019,000   ← total_amount - total_deduction_amount
─────────────────────────────────────────────────
```

**種別プルダウン**:
```
安全協力会費 / 材料費立替 / 駐車場代 / 法定福利費 / その他
```

**率入力時**:
- `amount` = `current_purchase × rate`（サーバー側で計算して保存）

#### 10.8.4 支払通知書 PDF レイアウト

```
支　払　通　知　書                           [CLAPロゴ]  株式会社クラップ

  下請業者名              :  △△内装株式会社 殿
  対象月                  :  2026年5月度
  弊社工事番号            :  26-1-001
  工事名                  :  ○○ビル工事

  ────────────────────────────────────────────────
  請求金額（税込）                ¥2,200,000
  ────────────────────────────────────────────────
  控除項目
    安全協力会費 (0.5%)           ¥   11,000
    材料費立替                    ¥  150,000
    駐車場代                      ¥   20,000
  控除合計                        ¥  181,000
  ────────────────────────────────────────────────
  当月支払決定額                  ¥2,019,000
  ────────────────────────────────────────────────

  支払予定日: ○○年○○月○○日
  支払方法  : 銀行振込

  ※ ご不明の点はご連絡ください。
```

---

### 10.9 編集履歴記録方針

控除の追加/削除は、**親 Invoice の `edit_histories`** に記録する：

```python
# 控除追加時
await record_edit_history(
    db, entity_type="invoice", entity_id=invoice.id,
    change_type=EditHistoryChangeType.update,
    field_changes={"deductions": {"before": None, "after": deduction_dict}},
    user_id=current_user.id,
)

# 控除削除時
await record_edit_history(
    db, entity_type="invoice", entity_id=invoice.id,
    change_type=EditHistoryChangeType.update,
    field_changes={"deductions": {"before": deduction_dict, "after": None}},
    user_id=current_user.id,
)
```

---

### 10.10 実装ファイル一覧

#### Backend

| ファイル | 変更内容 |
|---|---|
| `backend/app/shared/models/enums.py` | `ProjectRole` / `InvoicePhase` / `DeductionType` 追加 |
| `backend/app/models/enums.py` | re-export リストに上記3 enum 追加 |
| `backend/app/models/invoice.py` | `Invoice` に5列追加 / `InvoiceDeduction` クラス追加 |
| `backend/app/models/project.py` | `project_role` 列追加 |
| `backend/app/schemas/invoice.py` | `InvoiceDeductionCreate` / `InvoiceDeductionRead` / `InvoiceRead` 追加列 |
| `backend/app/schemas/project.py` | `ProjectRead` / `ProjectUpdate` に `project_role` 追加 |
| `backend/app/modules/report/services/deduction_service.py` | 新規（控除計算・合計更新ロジック） |
| `backend/app/modules/report/routers/invoices.py` | 発行時スナップショット転記処理追加 |
| `backend/app/modules/report/routers/exports.py` | `payment-notice-pdf` エンドポイント追加 |
| `backend/app/modules/project/router.py` | `PATCH /projects/{id}/role` エンドポイント追加 |
| `backend/app/modules/report/services/pdf_export.py` | `generate_payment_notice_pdf()` 追加 |
| `backend/alembic/versions/R1_invoice_role_phase_deductions.py` | 新規マイグレーション |

#### Frontend

| ファイル | 変更内容 |
|---|---|
| `frontend/src/app/projects/page.tsx` | 役割バッジ表示 / タブ絞り込み追加 |
| `frontend/src/app/projects/[id]/invoice/[invoice_id]/page.tsx` | 出来高セクション / 控除セクション追加 |
| `frontend/src/types/invoice.ts` | `InvoiceDeductionRead` / `InvoiceRead` 型追加 |
| `frontend/src/types/project.ts` | `project_role` 型追加 |

---

## 8. 変更ファイル一覧（予定）

### Backend
- `backend/app/shared/models/enums.py` — UserRole 変更なし
- `backend/app/models/user.py` — `roles`, `stamp_text`, `stamp_style` 追加
- `backend/app/schemas/user.py` — `UserCreate/Update` roles[] 対応
- `backend/app/shared/services/permissions.py` — 新規（has_role ヘルパー）
- `backend/app/modules/auth/router.py` — roles 対応
- `backend/app/modules/admin/` — 新規（ユーザー・印影・承認ルート等）
- `backend/app/modules/project/ledger_router.py` — 5枠対応
- `backend/alembic/versions/w7x8y9z0a1b2_multi_role_stamp_ledger.py` — 新規migration

### Frontend
- `frontend/src/app/admin/page.tsx` — 新規（admin.html準拠統合ページ）
- `frontend/src/app/admin/*/page.tsx` — 廃止（/admin に統合）
- `frontend/src/app/projects/[id]/page.tsx` — 工事台帳コンテンツ統合
- `frontend/src/app/projects/[id]/ledger/page.tsx` — 廃止
- `frontend/src/app/projects/[id]/history/page.tsx` — 5W1H改修
- `frontend/src/components/layout/AppShell.tsx` — サイドバー変更
- `frontend/src/modules/project/ProjectSubNav.tsx` — 工事台帳タブ削除
- `frontend/src/types/auth.ts` — roles[] 対応
